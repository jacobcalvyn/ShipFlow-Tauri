name: Build Windows EXE

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Install frontend dependencies
        run: npm ci

      - name: Run frontend tests
        run: npm test

      - name: Build Windows frontend bundle
        run: npm run build

      - name: Build Windows executable bundle
        run: npm run build:bundle:nsis

      - name: Upload portable executable
        uses: actions/upload-artifact@v6
        with:
          name: shipflow-desktop-windows-portable
          path: src-tauri/target/release/shipflow3-tauri.exe
          if-no-files-found: error

      - name: Upload NSIS installer
        uses: actions/upload-artifact@v6
        with:
          name: shipflow-desktop-windows-installer
          path: src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error
