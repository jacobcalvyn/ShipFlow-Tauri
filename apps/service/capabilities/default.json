{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the standalone ShipFlow Service settings window. Native access stays behind explicit Tauri commands.",
  "windows": ["service-settings"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
